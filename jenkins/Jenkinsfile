#!/bin/groovy

timestamps {
	node('felix-sierra') {
        stage('checkout') {
            checkout scm

            sh('make print-versions')
            sh('./configure --enable-xamarin')
            sh('make reset')
        }
    }
}

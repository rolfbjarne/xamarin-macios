#!/bin/groovy

timestamps {
	node('felix-sierra') {
        stage('checkout') {
            checkout scm

            sh('env')
            sh('pwd')
            sh('ls -la')
            sh('./jenkins/build-package.sh')
        }
    }
}
